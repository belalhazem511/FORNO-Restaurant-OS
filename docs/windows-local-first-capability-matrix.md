# Windows local-first capability matrix

This is an implementation-status matrix, not a claim that every web mutation is synchronized. Verified command slices currently include customers, products/media references, orders, checkout/cancellation, shifts/drawer, print jobs/settings, and manual stock adjustments. Product media bytes transfer through paired authenticated, content-hash-verified endpoints. Existing web/PWA behavior remains unchanged. A workflow marked pending must not be represented to staff as synchronized.

| Page/workflow | Local read/write | Local transaction boundary | Existing permission | Typed sync command | Idempotency/dependency | Conflict behavior | Evidence |
|---|---|---|---|---|---|---|---|
| `/admin/customers` — `customers.list` | Local PGLite read | Query only | Authenticated user; rows scoped by `user_uid` | None required | Read-only | N/A | Existing `customers.test.ts` |
| `/admin/customers` — `customers.create` | Local write in desktop mode; web path unchanged | Customer + global mapping + audit + outbox in one PGLite transaction | Existing authenticated procedure; central revalidation requires active branch assignment | `customers.create` v1 | UUID operation and idempotency keys; queued edits depend on prior pending commands | Rejected commands remain locally visible; central conflict retained | `customer-sync.test.ts`; `commands.test.ts` |
| `/admin/customers` — `customers.update` | Local write in desktop mode; web path unchanged | Customer + mapping revision + audit + outbox in one PGLite transaction | Existing authenticated procedure; central revalidation requires active branch assignment | `customers.update` v1 | UUID operation/key; depends on pending customer commands; base revision | Stale central revision creates append-only Needs Review record and preserves both snapshots | `customer-sync.test.ts`; `commands.test.ts` |
| `/admin/customers` — `customers.delete` | Local delete in desktop mode; web path unchanged | Delete + mapping revision + audit + tombstone outbox in one PGLite transaction | Existing authenticated procedure; central revalidation requires active branch assignment | `customers.delete` v1 | UUID operation/key; depends on pending customer commands; base revision | Stale edits preserve both versions for Needs Review; pull applies tombstones without discarding mappings | `customer-sync.test.ts`; `commands.test.ts` |
| `/admin/products` — `products.create` and `products.update` | Local PGLite write | Product + global mapping + audit + outbox in one local transaction | Existing authenticated procedure; central active-branch assignment revalidation | `products.create/update` v1; opaque image key reference only | UUID operation/key; same-product pending command dependencies; base revision | Stale catalog edits create Needs Review; central snapshots omit local integer IDs | New customer-sync/product command tests; product regression tests |
| `/admin/products` — `products.delete` and product image upload/replace/remove | Delete and image-key edits enqueue local commands; image bytes transfer separately | Product + mapping revision + audit + outbox transaction; image file written first and only replacement file removed after DB success | Existing authenticated/image permission behavior; paired device authenticates media transport | `products.delete`; product image metadata as `products.update`; content-hash media transfer | UUID operation/key; media key and SHA-256; same-product dependencies | Tombstones synchronize; media content is immutable at an opaque key and conflicting bytes are rejected | Sync and product-media storage tests; packaged cross-device media test pending |
| `/admin/pos`, `/admin/orders`, checkout, shifts, transactions, payment methods | Local PGLite workflow; paired sync is partial | Existing order/checkout/shift transactions enqueue supported commands atomically | Existing router permissions; central handlers revalidate supported operations | `orders.create/transition/update`, `checkout.pay/cancel`, `shifts.open/drawer_adjust/close` | Operation UUID, idempotency key, cross-domain dependencies for supported commands | Supported order/shift revision and overlap conflicts enter Needs Review; unsupported mutation rows remain pending | `commands.test.ts`, `customer-sync.test.ts`; broad packaged acceptance pending |
| `/admin/inventory` — manual positive/opening and negative stock adjustments | Local PGLite ledger mutation; web behavior unchanged | Movement, WAC/balance, audit, mapping and outbox share the caller transaction in desktop mode | `inventory:adjust`; negative override additionally requires `inventory:override` and ingredient negative-stock allowance | `inventory.adjust` v1; central handler appends an authoritative movement; pull imports movement and recomputes exact destination WAC | Idempotency key; dependencies on ingredient and location UUID mappings; no mutable balance replication | Central negative adjustment locks/revalidates stock; insufficient/unsafe imported deductions roll back the page and cursor | Sync command/local-outbox tests and existing inventory ledger tests; peer-package acceptance pending |
| `/admin/suppliers` — supplier create/update/archive | Local PGLite mutation, including audit and outbox in one transaction | Supplier row + UUID mapping/revision + audit + typed outbox | `supplier:manage`; central handler revalidates active branch assignment | `suppliers.create/update/archive` v1 | UUID operation/idempotency; updates depend on same-supplier commands and carry base revision | Stale revision and duplicate code enter Needs Review; archive is a synchronized inactive snapshot | `procurement.test.ts`, `customer-sync.test.ts`, `commands.test.ts` |
| `/admin/inventory` — `createIngredient`, `updateIngredient`, `archiveIngredient` | Local PGLite mutation in desktop mode; web behavior unchanged | Ingredient + local identity/revision + audit + typed outbox share the existing caller transaction; creation initializes a zero balance only | `inventory:configure`; paired central handler validates active branch assignment and permission | `inventory.ingredient_create/update/archive` v1; snapshots resolve category, location and unit by branch-scoped stable codes | UUID operation/idempotency; edits depend on mapped references and carry base revision | Revision mismatch and duplicate SKU preserve local payload/server snapshot in Needs Review; pull maps ingredient to device-local ID and does not replicate balance | `customer-sync.test.ts`; central create/update/archive retry and zero-movement assertions in `commands.test.ts` |
| `/admin/inventory` — `adjust` opening balance/manual positive or negative adjustment | Local PGLite ledger workflow | Movement, WAC/balance, audit, global mapping and outbox share the caller transaction | `inventory:adjust`; negative override also requires `inventory:override` | `inventory.adjust` v1 | Movement idempotency key; ingredient/location mapping dependencies | Central revalidates stock under lock; peer appends the authoritative movement and derives its own balance/WAC | `customer-sync.test.ts`, `commands.test.ts`, `inventory.test.ts` |
| `/admin/inventory` — `createRecipe`, `activateRecipe` and automatic retirement of prior active version | Local PGLite workflow; recipe mutations themselves are stock-neutral | Immutable recipe version/components + mappings + audit + outbox in the caller transaction | `recipe:manage`; central handler revalidates actor, branch, recipe revision and menu/ingredient references | `inventory.recipe_create/activate` v1; UUID references include menu item, variant, ingredient, location, unit and optional modifier option | Recipe creation depends on referenced catalog/inventory entities; activation depends on draft creation and base revision | Version/revision or active-history conflict is retained in Needs Review; peer resolves local IDs, recalculates exact rational base quantities, and imports immutable components | `customer-sync.test.ts`, `commands.test.ts`, `inventory.test.ts`; repeated recipe changes assert zero stock movements |
| Inventory locations, ingredient categories, units of measure and package conversions | Read/configuration records are seeded or provisioned; no existing tRPC mutation in `inventoryRouter` edits these records | No current user mutation transaction to synchronize | Existing configuration/bootstrap rules | No command registered because no current mutation workflow exists | None | Provisioned references resolve by branch-scoped stable codes; future edit workflow needs its own command contract | `inventory.ts` procedure inventory; schema/configuration tests |
| Purchase orders, receiving, supplier returns, transfers, counts and order consumption | Existing local business workflows | Existing workflow-specific append-only transactions | Existing inventory/procurement permissions | **Pending in this batch** | Existing business idempotency only | Must preserve immutable movements/snapshots and route unsafe central conflicts to Needs Review | Existing procurement, receiving, return, transfer, count and inventory suites; cross-device coverage remains pending |
| `/print/[jobId]`, `/offline-print/[documentId]`, printing settings/reprints | Local document/print workflow; supported job/settings events synchronize | Print-job and preference workflows enqueue their supported commands atomically | Existing print permissions; central handler revalidates supported print operations | `printing.request/transition/settings_update` | Existing print keys and order/shift/job dependencies | Supported events reconcile without automatic printing on pull; some document-history coverage remains incomplete | Printing and sync tests; broad packaged acceptance pending |
| `/admin/storage`, `/admin/sync` — device setup, pairing, manual/periodic sync | Local identity, protected device credential, local queue and cursor | Pairing and command inbox/change-log transactions | Owner-authorized pairing; setup operations protected by local Electron token | Customer command transport only | Operation UUID, device ID, idempotency, payload hash, dependencies | Explicit accepted/already-applied/needs-review/rejected/retry-later results; customer pull conflicts preserved | Pairing tests from earlier milestone; new customer command tests |
| Login, staff, roles, permissions, branch/register assignments | Existing local authentication/database functionality; sync pending | Existing auth/domain boundaries | Existing authorization model | **Pending protected auth channel** | **Pending** | Permission conflicts must not broaden access; not implemented yet | No desktop multi-device evidence |

## Mutation coverage still required

Supported command coverage remains intentionally partial. This batch adds typed synchronization for existing ingredient create/update/archive, opening/manual stock adjustment, and recipe create/activation/retirement mutations. The current inventory router exposes no editable location, category, unit, or package-conversion mutation, so no new business operation was introduced for those tables. Purchase orders, receiving, supplier returns, transfers, counts, standalone transaction/payment-method mutations, and protected user/permission synchronization remain uncovered and must not be presented as cross-device synchronized. Read/preview procedures do not require outbox commands.

The full operation-by-operation catalogue, server permission rule, dependency, conflict rule, and test evidence must be completed as each domain is instrumented. No raw table replication is used. A local record is not centrally synchronized merely because it exists in PGLite.

## Current transport contract

- `shifts.open` is now a typed command. Central processing revalidates the paired register and active cashier assignment, detects register/cashier overlap, creates mapping/audit/change event transactionally, and marks overlaps Needs Review; shift snapshots import locally with overlap protection.
- `shifts.drawer_adjust` is now a typed command dependent on the shift-open operation. Central authorization rechecks `cash:adjust`; cash movement, completed financial transaction, mapping, audit, inbox result, and ordered change are written in one transaction. Denials are audited without financial effects.
- `shifts.close` is now a typed command. Local close, shift mapping revision, audit, and outbox are atomic; dependency discovery crosses domains and waits for pending commands referencing the shift UUID. Central recalculates expected drawer cash and accepts close only when the device snapshot matches; mismatches remain open centrally and enter Needs Review. Accepted close events carry only close totals/status fields to pull import.
- `inventory.adjust` synchronizes only existing opening/manual-positive and manual-negative adjustment operations. The local transaction writes the ledger movement and outbox together; central processing resolves ingredient/location UUIDs, rechecks authorization and stock under lock, and appends its own movement. Pull carries the authoritative movement snapshot, never a balance row, and applies exact quantity/WAC changes with the importing device's local IDs. This does not cover ingredient/recipe configuration or procurement workflows.
- The focused isolated PGLite characterization imports a shift and drawer movement onto a second device using different local branch, register, and user IDs while retaining global identities and exactly one local movement/transaction per device.

- Customer and product deletion are synchronized as typed tombstones, with local deletion, mapping revision, audit, and outbox committed together. Peers retain the UUID mapping after deleting the local row; rejected payload hashes are audited without applying business data.

- Customer deletion now uses a typed `customers.delete` tombstone. The local row is removed only in the same transaction that records its mapping revision, audit, and outbox operation; central pull preserves the global mapping while applying the tombstone.

- Device authentication uses the paired device UUID and bearer secret; the central database stores only the SHA-256 credential hash.
- Customer command payloads carry stable customer UUIDs and customer values, not cross-device integer IDs.
- Change pulls are ordered and limited to 200 events per page. Customer snapshots exclude credentials and financial data.
- Local application and cursor advancement share a transaction. Pending local customer edits are preserved with a server snapshot in Needs Review.
- The Electron process owns the protected credential and central URL. The renderer receives only narrow status/manual-sync IPC methods.
- Cursor-incompatible full snapshots, resumable snapshot checksums, and non-customer domain exporters/importers are not implemented yet.
